<h1 class="text-center my-4">Article Listing Page</h1>

<!-- Table Container -->
<div class="container">
  <table class="table table-bordered table-striped">
    <thead class="thead-dark">
      <tr>
        <th>Title</th>
        <th>Description</th>
        <th colspan="3" class="text-center">Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @articles.each do |article| %>
        <tr>
          <td><%= article.title %></td>
          <td><%= article.description %></td>
          <td><%= link_to 'View', article_path(article), class: 'btn btn-info btn-sm' %></td>
          <td><%= link_to 'Edit', edit_article_path(article), class: 'btn btn-warning btn-sm' %></td>
          <td><%= link_to 'Delete', article_path(article), method: :delete, class: 'btn btn-danger btn-sm', data: { confirm: 'Are you sure you want to delete this article?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <!-- Button to Create New Article -->
  <div class="text-center mt-3">
    <%= link_to 'New Article', new_article_path, class: 'btn btn-primary' %>
  </div>
</div>