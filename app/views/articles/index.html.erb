<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home - Articles</title>
  <%= tag.link rel: "stylesheet", href: "https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" %>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <%= link_to 'Alpha Blog', root_path, class: 'navbar-brand' %> <!-- Link to Home page -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <%= link_to 'Home', root_path, class: 'nav-link active' %> <!-- Link to Home page -->
          </li>
          <li class="nav-item">
            <%= link_to 'About', about_path, class: 'nav-link' %> <!-- Link to About page -->
          </li>
          <li class="nav-item">
            <%= link_to 'Articles', articles_path, class: 'nav-link' %> <!-- Link to Articles page -->
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Add Article Button (Top of Page) -->
  <div class="text-center my-4">
    <%= link_to 'New Article', new_article_path, class: 'btn btn-primary' %>
  </div>

  <h1 class="text-center my-4">Article Listing Page</h1>

  <!-- Cards for Articles -->
  <div class="container">
    <div class="row">
      <% @articles.each do |article| %>
        <div class="col-md-4 mb-4"> <!-- Each card will take up 4 columns on medium-sized screens -->
          <div class="card">
            <div class="card-body">
              by <%= article.user.username if article.user %>
              <h5 class="card-title"><%= article.title %></h5>
              <p class="card-text"><%= article.description %></p>

              <!-- Action Buttons (Inside the Card) -->
              <div class="text-center mt-3">
                <%= link_to 'View', article_path(article), class: 'btn btn-info btn-sm' %>
                <%= link_to 'Edit', edit_article_path(article), class: 'btn btn-warning btn-sm' %>
                <%= link_to 'Delete', article, class: 'btn btn-danger btn-sm', data: { turbo_method: :delete, turbo_confirm: 'Are you sure you want to delete this article?' } %>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Bootstrap JS (Optional for interactivity like togglers) -->
  <%= tag.script src: "https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" %>
</body>
</html>
